pipeline {
agent any
       stages{
        stage('Clone'){
             steps{  
                 git branch: 'ismail', url: 'https://github.com/EmnaFerchichi/DevopsRepos.git'
             }
        }
       
        stage('MVN CLEAN') {
            steps {
                sh 'mvn clean'
                 
            }
        }
