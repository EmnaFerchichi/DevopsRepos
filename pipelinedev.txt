pipeline {
	agent any 
       stages{
        stage('Clone'){
             steps{  
                 git branch: 'raniab', url: 'https://github.com/EmnaFerchichi/DevopsRepos.git'
             }
        }
           stage("Build") {
                 steps {
			sh "mvn compile"
}}
   }
}