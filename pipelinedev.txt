pipeline {
	agent any 
       stages{
        stage('Clone'){
             steps{  
                 git branch: 'Emna', url: 'https://github.com/EmnaFerchichi/DevopsRepos.git'
             }
        }
           stage("Build") {
                 steps {
			sh "mvn compile"
}}
   }
}